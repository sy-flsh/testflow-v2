import { PageShell } from "@/components/layout/page-shell";

export default function SettingsPage() {
  return (
    <PageShell
      title="워크스페이스 설정"
      description="일반 정보, 멤버, 역할 및 권한, 위험구역 화면이 들어갈 영역입니다."
    />
  );
}
